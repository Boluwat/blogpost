const userController = require("./user.controller");
const blogPostControllers = require("./blogPost.controller");
const commentControllers = require("./comment.controller");

module.exports = {
  userController,
  blogPostControllers,
  commentControllers,
};
